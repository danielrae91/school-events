import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'TK Newsletter - School Events Calendar',
  description: 'Automated school newsletter events feed - subscribe to stay updated with all school events',
  keywords: 'school events, calendar, newsletter, ical, feed',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
